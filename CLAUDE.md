# CLAUDE.md

This project's agent instructions live in a single, tool-neutral file so every
agent (Claude Code, Cursor, Copilot, Codex, …) reads the same rules. Claude
Code auto-loads this file; the line below imports the canonical instructions.

@AGENTS.md
