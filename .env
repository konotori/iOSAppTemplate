# Template config — edit these, then run:  make new-app
# Use a HYPHEN-FREE name (e.g. MyApp). Hyphens become underscores in
# Swift type names and trip the SwiftLint `type_name` rule.
NEW_PROJECT_NAME=
NEW_BUNDLE_ID=
